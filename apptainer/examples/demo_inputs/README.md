Place your real input PDBs in this directory and update the example YAMLs before submitting.

The bundled example YAMLs deliberately point here so the bind paths are consistent inside the container:

- `/workspace/apptainer/examples/demo_inputs/antigen_example.pdb`
- `/workspace/apptainer/examples/demo_inputs/target_example.pdb`
